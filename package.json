{
  "name": "camp-scheduler-netlify-backend",
  "version": "1.0.0",
  "description": "Netlify Functions (classic) + Netlify Blobs sync backend for the Camp Scheduler",
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
